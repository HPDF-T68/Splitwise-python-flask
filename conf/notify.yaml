email:
  default: "smtp"
  providers:
    smtp:
      hostname: "smtp.gmail.com"
      password:
        secretKeyRef:
          key: notify.smtp.password
          name: hasura-secrets
      port: 587
      username:
        secretKeyRef:
          key: notify.smtp.username
          name: hasura-secrets
    sparkPost:
      apiKey:
        secretKeyRef:
          key: notify.sparkpost.key
          name: hasura-secrets