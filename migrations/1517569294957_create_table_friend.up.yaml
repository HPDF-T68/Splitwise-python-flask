- args:
    sql: CREATE TABLE "friend"("s.n" bigserial NOT NULL, "uid" numeric NOT NULL, "friend_id"
      numeric NOT NULL, PRIMARY KEY ("s.n") )
  type: run_sql
- args:
    name: friend
  type: add_existing_table_or_view
