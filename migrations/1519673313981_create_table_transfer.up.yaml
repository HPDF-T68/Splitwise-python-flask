- args:
    sql: CREATE TABLE "transfer"("t_id" serial NOT NULL, "from" text NOT NULL, "to"
      text NOT NULL, "credit" bigint NOT NULL, PRIMARY KEY ("t_id") )
  type: run_sql
- args:
    name: transfer
  type: add_existing_table_or_view
