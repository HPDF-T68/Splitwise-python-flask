- args:
    sql: CREATE TABLE "signup"("uid" numeric NOT NULL, "email" text NOT NULL, "mobile"
      text NOT NULL, "currency" text NOT NULL, "money" bigint NOT NULL, "username"
      text NOT NULL, PRIMARY KEY ("uid") )
  type: run_sql
- args:
    name: signup
  type: add_existing_table_or_view
