- args:
    sql: CREATE TABLE "friend"("s.n" bigserial NOT NULL, "uid" text NOT NULL, "friend_id"
      text NOT NULL, PRIMARY KEY ("s.n") )
  type: run_sql
- args:
    name: friend
  type: add_existing_table_or_view
